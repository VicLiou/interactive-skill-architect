# Skill 主要骨架 (Primary Skeleton)

# 這是所有產出 Skill 的主要骨架，必須符合 style-guide.md §8 的「SKILL.md 標準骨架」順序：
#   frontmatter → 角色定義句 → 主體工作流程(自由) → ## Gotchas → 迭代註腳。
# 資料夾層級依 §9、效率依 §11；若會輸出結果統計，依 §10 在 assets/ 放結果報告模板。
# Agent 在 Phase 3 時，以此為基礎，結合 Q1~Q6 的回答量身打造。
# 產生檔案時，請移除此開頭註解。

---
name: {{skill_name}}
description: |
  {{一句話說明功能}}。
  當使用者說「{{觸發詞A}}」、「{{觸發詞B}}」、「{{觸發詞C}}」時觸發。
  不要用於 {{排除情境}}。
metadata:
  pattern: {{根據 Q2 的描述填入最接近的模式名稱，若為全新模式則自訂描述性名稱}}
  type: {{根據 Q3 填入英文名稱，若不在 9 種清單中則自訂描述性英文名稱}}
---

# {{Skill 標題}}

{{根據 Q2 描述的運作方式，用一段話定義 Agent 的角色與行為準則。使用祈使句。}}

{{【選用・檔案結構地圖】僅當檔案較多時才加（含 `scripts/` 或 `references/` ≥3 份）；小技能請刪除本節。若保留，內容必須與實際資料夾結構一致（依 style-guide.md §8）。範例：}}

## 檔案結構 (File Map)

```
{{skill-name}}/
├── SKILL.md
├── references/
│   └── {{檔名}}.md          {{一句用途}}
└── assets/
    └── {{檔名}}.md          {{一句用途}}
```

## 執行流程 (Execution Flow)

{{主體工作流程為「自由區」：標題與內部結構可依模式調整（執行流程／審查協議／Phase 階段…），步驟數量不限。以下為結構指引：}}

Step 1: {{步驟描述}}。{{若此步驟需要載入外部檔案，直接在這裡標明：載入 `references/xxx.md`（漸進揭露：只在用到時載入，勿在開頭全載）。}}
  - **放行條件**：{{若有硬性閘門，寫在這裡。若無則刪除此行。}}

Step 2: {{步驟描述}}。
  - **放行條件**：{{若有硬性閘門，寫在這裡。若無則刪除此行。}}

{{繼續新增步驟。步驟數量完全依照使用者需求決定，不限數量。連續且中間無放行條件的步驟可合併（§11），但禁止合併跨越任何硬性放行條件。}}

{{【選用】若本 Skill 結束時會輸出結果統計／彙總報告：依 style-guide.md §10，在 assets/ 建立 `result-report-template.md`，並在產出統計的步驟旁綁定載入，例如：}}
{{  Step N: 將結果依 `assets/result-report-template.md` 彙整為報告後交付。}}

## Gotchas (踩過的坑)

> [!WARNING]
> 過去的實戰經驗顯示，這項任務容易在以下環節出錯。請務必牢記並避開這些陷阱：

- **{{粗體標題}}**：{{具體描述失敗情境、正確做法、以及為什麼這樣做。}}
{{根據 Q5 的回答填入。若使用者選擇 B，Agent 必須根據領域推薦至少 2 條。}}

<!-- 🔄 持續迭代提醒：每次 Agent 執行此技能犯錯時，請回來補充新的 Gotcha。
     Skill 的價值不在你寫了多少，而在你有沒有告訴 Agent「它原本不知道、或老是做錯的那件事」。 -->
